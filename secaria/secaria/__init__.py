"""Secaria — an agentless security posture assessment toolkit.

The narrow first slice focuses on Active Directory: collect facts from a
domain controller over LDAP (read-only), evaluate a set of data-defined
rules, and render a report with findings mapped to MITRE ATT&CK, CIS, and
DISA STIG.
"""

__version__ = "0.1.0"
